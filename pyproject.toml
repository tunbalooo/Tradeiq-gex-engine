[project]
name = "tradeiq-gex-engine"
version = "1.2.0"
description = "Multi-market futures GEX and confluence dashboard"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
