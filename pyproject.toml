[project]
name = "tradeiq-gex-engine"
version = "0.1.0"
description = "Local NQ GEX and confluence dashboard"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
