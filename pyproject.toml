[project]
name = "tradeiq-gex-engine"
version = "1.3.0"
description = "Fast-switch multi-market futures GEX and confluence dashboard"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
