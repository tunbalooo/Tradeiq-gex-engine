[project]
name = "tradeiq-gex-engine"
version = "3.1.7"
description = "Multi-market futures GEX radar, confluence and adaptive execution dashboard"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
